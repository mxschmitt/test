name: "tests repro"

on:
  push:
    branches:
      - main

jobs:
  arm:
    name: upload
    runs-on: macos-14-xlarge
    environment: Test
    steps:
    - uses: actions/checkout@v4
    - run: arch
