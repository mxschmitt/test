name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  read_playwright_version:
    runs-on: ubuntu-latest
    outputs:
      playwright-version: ${{ steps.get_playwright_version.outputs.playwright-version }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm ci
      - run: |
          playwright_version=$(npx playwright --version)
          echo "::set-output name=playwright-version::$playwright_version"
        id: get_playwright_version
  playwright:
    name: 'Playwright Tests'
    runs-on: ubuntu-latest
    needs: read_playwright_version
    container:
      image: mcr.microsoft.com/playwright:v${{ needs.read_playwright_version.outputs.playwright-version }}-noble
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Install dependencies
        run: npm ci
      - name: Run your tests
        run: npx playwright test
        env:
          HOME: /root