name: CI
on: [push]
jobs:
  build1:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    name: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v2
     # Debugging session
    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
      timeout-minutes: 10
